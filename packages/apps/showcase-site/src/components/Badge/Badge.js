import * as React from 'react';
import * as styles from './Badge.module.scss';

const Badge = () => {
    return (
        <span className={styles.badge}>BadgeName</span>
    )
}

export default Badge;